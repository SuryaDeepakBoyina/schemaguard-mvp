# Milestone: v0.1-mvp

This milestone captures work for the SchemaGuard Health AI MVP:

- Core validation endpoints (`/validate-record`, `/fhir-check`, `/suggest-fixes`).
- Prometheus metrics and basic Grafana readiness.
- Docker and local dev workflow.
- Basic unit tests and CI workflow.

Target: v0.1-mvp
